def run(app):
    # Put some tests here. For example,

    #with app.test_client() as c:
    #    with c.session_transaction() as sess:
    #        sess['user_id'] = '123'

    #    c.set_cookie('/', 'csrf', 'abc')

    #    create_list_resp = c.post(BASE_URL + '/grocery-lists',
    #        data=json.dumps({
    #            'name': 'some list'
    #        }),
    #        headers={
    #            'content-type': 'application/json',
    #            'X-Csrf': 'abc'
    #        })

    #    assert create_list_resp.status_code == 201

    pass
